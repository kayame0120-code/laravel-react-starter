# 改行は常に LF。Windows 側の VSCode で開いても CRLF にしない。
* text=auto eol=lf

# シェルスクリプトは CRLF だとコンテナ内で起動できない
*.sh  text eol=lf

# バイナリは変換禁止（gpg鍵が壊れるとイメージがビルドできない）
*.gpg   binary
*.woff  binary
*.woff2 binary
*.png   binary
*.jpg   binary
*.ico   binary
